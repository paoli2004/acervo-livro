import { Entity } from "typeorm";

@Entity({ name: 'livros' })
export class Livros {
    // Define your entity properties here, for example:
    // @PrimaryGeneratedColumn()
    // id: number;

    // @Column({type: 'text', nullable: true})
    // name: string;

    // @Column({type: 'boolean', default: true})
    // population: number;
}
